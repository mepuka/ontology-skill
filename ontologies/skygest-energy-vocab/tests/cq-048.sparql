# CQ-048: What are all ConceptSchemes after the gap expansion?
# Expected: non_empty
# Use case: UC-011

PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

SELECT ?scheme ?label WHERE {
  ?scheme a skos:ConceptScheme ; skos:prefLabel ?label .
} ORDER BY ?label
