# energy-intel — CQ Test Manifest (Step 7.5)
#
# parse_status: ok         — `rdflib.plugins.sparql.prepareQuery` accepts the file.
# fixture_run_status:
#   skipped                — no fixture built in Requirements phase. Fixtures land in
#                            ontologies/energy-intel/tests/fixtures/{cq-id}.ttl after
#                            the ontology-architect produces the first TBox slice.
#   pass / fail            — set once fixture is built and query runs against it.
#
# entailment_regime is the minimum regime a downstream reasoner must enable for the
# query to return the contractually-specified rows. CQ-013 / CQ-014 use rdfs:subClassOf*
# property paths, which work under RDFS entailment; Hermit DL is acceptable and
# strictly stronger. CQ-009 is a constraint query whose expected answer is 0 rows;
# any reasoner that materializes negation-as-failure via `FILTER NOT EXISTS` satisfies.
#
# expected_results_contract is the downstream-binding promise: a test-runner can verify
# it by asserting column shape + row-count band + sample row presence against the
# fixture. See _shared/cq-traceability.md § expected-results contract.

preflight_summary:
  total: 30
  parse_ok: 30
  parse_fail: 0
  fixtures_built: 30
  fixtures_passing: 30
  fixtures_failing: 0
  fixtures_skipped: 0   # V2 fixtures landed 2026-05-04
  architect_session: "2026-04-22 (V0 post-OEO-verification); V1 architect 2026-04-27"
  fixture_builder: "ontologies/energy-intel/scripts/build_fixtures.py"
  v1_requirements_session: "2026-04-27 (CQ-015..019 added; preflight ok)"
  v1_architect_session: "2026-04-27 (V1 fixtures + axioms + shapes landed)"
  v2_requirements_session: "2026-05-04 (CQ-N1..N8, CQ-T1..T3 added; preflight ok 11/11)"
  v2_architect_session: "2026-05-04 (V2 fixtures + axioms + shapes landed)"

tests:

  - id: CQ-001
    file: tests/cq-001.sparql
    kind: select
    expected: "non-empty when the target post has any CMC; empty for plain-text posts"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
      Against a fixture with a single post evidencing one CMC, returns exactly that CMC IRI.
      Against a fixture where the target post has no ei:evidences, returns 0 rows.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-001.ttl
    entailment_regime: simple
    fixture_rationale: "TBox is pre-architect; fixture built in Slice 1 of ontology-architect handoff."

  - id: CQ-002
    file: tests/cq-002.sparql
    kind: select
    expected: "exactly 1 row for any valid Post"
    expected_results_contract: |
      Columns: ?expert.
      Cardinality: exactly 1 on a valid fixture.
      Zero rows indicates the Post → Expert cardinality axiom is not enforced by the ontology.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-002.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-003
    file: tests/cq-003.sparql
    kind: select
    expected: "0..1 rows"
    expected_results_contract: |
      Columns: ?variable.
      Cardinality: 0..1 (unresolved CMC returns 0).
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-003.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-004
    file: tests/cq-004.sparql
    kind: select
    expected: "0..1 rows"
    expected_results_contract: |
      Columns: ?series.
      Cardinality: 0..1.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-004.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-005
    file: tests/cq-005.sparql
    kind: select
    expected: "0..1 rows"
    expected_results_contract: |
      Columns: ?distribution.
      Cardinality: 0..1.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-005.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-006
    file: tests/cq-006.sparql
    kind: select
    expected: "0..n rows; 0 is a valid cold-start outcome"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
      Against a fixture with two CMCs referencing the same Variable, returns both.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-006.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-007
    file: tests/cq-007.sparql
    kind: select
    expected: "0..n distinct rows"
    expected_results_contract: |
      Columns: ?expert.
      Cardinality: 0..n; DISTINCT applied.
      Two Experts making claims about the same Variable must both appear.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-007.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-008
    file: tests/cq-008.sparql
    kind: select
    expected: "0..n rows (full cross-expert join payload)"
    expected_results_contract: |
      Columns: ?cmc ?post ?expert ?assertedValue ?assertedUnit ?assertedTime.
      Cardinality: 0..n.
      OPTIONAL columns may be null per row; ?cmc, ?post, ?expert must bind on every row.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-008.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-009
    file: tests/cq-009.sparql
    kind: select
    expected: "exact_0 rows on a well-formed fixture"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: exactly 0.
      Any row is a violation: a CMC exists without an evidencing media artefact.
      Test runner fails when this query returns > 0 rows.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-009.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-010
    file: tests/cq-010.sparql
    kind: select
    expected: "exactly 1 row with OPTIONAL columns"
    expected_results_contract: |
      Columns: ?agent ?dist ?dataset ?var ?series (all OPTIONAL).
      Cardinality: exactly 1.
      A freshly-extracted CMC with no resolutions returns one row where all columns are null.
      A fully-resolved CMC returns one row with all five columns populated.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-010.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-011
    file: tests/cq-011.sparql
    kind: select
    expected: "0..n rows (multi-speaker segments)"
    expected_results_contract: |
      Columns: ?expert.
      Cardinality: 0..n.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-011.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect; podcast ingest lane is planned not live per scope.md."

  - id: CQ-012
    file: tests/cq-012.sparql
    kind: select
    expected: "0..n rows"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-012.ttl
    entailment_regime: simple
    fixture_rationale: "Pre-architect."

  - id: CQ-013
    file: tests/cq-013.sparql
    kind: select
    expected: "0..n rows; transitive concept walk"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
      Uses SPARQL 1.1 property path (skos:broader | rdfs:subClassOf)* — works under simple
      entailment regime (no reasoner required). Path union accommodates both V0's hand-seeded
      skos:Concept hierarchy and the post-MIREOT OEO class hierarchy reached via punning.
      Querying a parent concept returns CMCs tagged with the concept or any transitively
      narrower/subclassed concept.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-013.ttl
    entailment_regime: simple
    fixture_rationale: "Rewritten 2026-04-22 to route classification through CMC-level ei:aboutTechnology (seven-facet Variable model deferred to V1). Fixture landing requires ontology-scout to produce at least a stub skos:Concept tree at imports/concept-subset.ttl, or ontology-architect to hand-seed a couple of concepts."

  - id: CQ-014
    file: tests/cq-014.sparql
    kind: select
    expected: "0..n distinct rows; two-hop walk via CMC-level concept tag"
    expected_results_contract: |
      Columns: ?post.
      Cardinality: 0..n; DISTINCT applied.
      Two-hop traversal (Post → CMC → concept) with transitive property path.
      Same simple-entailment regime as CQ-013.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-014.ttl
    entailment_regime: simple
    fixture_rationale: "Rewritten 2026-04-22 alongside CQ-013; Variable hop removed per seven-facet deferral. Fixture prerequisites identical to CQ-013."

  - id: CQ-015
    file: tests/cq-015.sparql
    kind: select
    expected: "0..n CMCs tagged with the parent OEO technology or any narrower descendant"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
      Transitive walk over ei:aboutTechnology/(skos:broader|rdfs:subClassOf)*.
      Against a fixture binding an OEO subtree class (here oeo:OEO_00020267 itself, the
      energy-technology root), returns every CMC tagged that class or any
      transitive descendant. Empty-path match suffices when fixture tags the root.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-015.ttl
    entailment_regime: simple
    fixture_rationale: "V1 OEO MIREOT subset (BFO+RO-stripped) wired in V1 architect (2026-04-27). Fixture tags CMC directly with oeo:OEO_00020267 because V0 BOT extract pulls upward parent path only, not descendants."

  - id: CQ-016
    file: tests/cq-016.sparql
    kind: select
    expected: "0..n CMCs whose ei:canonicalUnit equals the bound qudt:Unit IRI"
    expected_results_contract: |
      Columns: ?cmc.
      Cardinality: 0..n.
      Direct lookup on ei:canonicalUnit (V1 new property; range qudt:Unit).
      Bound IRI is unit:GigaW (NOT unit:GW — V0 manifest's IRI was wrong; QUDT
      2.1 uses SI-prefix-as-word naming).
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-016.ttl
    entailment_regime: simple
    fixture_rationale: "ei:canonicalUnit minted in V1 architect (2026-04-27); fixture binds unit:GigaW from the qudt-units-subset import."

  - id: CQ-017
    file: tests/cq-017.sparql
    kind: select
    expected: "0..n (CMC, unit) pairs whose unit hasQuantityKind matches"
    expected_results_contract: |
      Columns: ?cmc ?unit.
      Cardinality: 0..n.
      Inference path: CMC -> ei:canonicalUnit -> qudt:hasQuantityKind.
      QUDT subset declares qudt:hasQuantityKind on every imported Unit;
      fixture only adds the CMC-side ei:canonicalUnit triple.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-017.ttl
    entailment_regime: simple
    fixture_rationale: "QUDT QuantityKind kept (per scout SQ-2 GO decision); CQ-017 stays should_have. Architect uses imported qudt:hasQuantityKind directly — no local ei:hasQuantityKind."

  - id: CQ-018
    file: tests/cq-018.sparql
    kind: select
    expected: "0..n distinct foaf:Person individuals bearing ei:EnergyExpertRole and authoring at least one Post"
    expected_results_contract: |
      Columns: ?person.
      Cardinality: 0..n; DISTINCT applied.
      Direct lookup on the role-bearer pattern.
      Confirms V1 Expert refactor structurally reaches the right entities.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-018.ttl
    entailment_regime: simple
    fixture_rationale: "ei:EnergyExpertRole minted in V1 architect (2026-04-27); fixture asserts foaf:Person + bfo:0000053 + EnergyExpertRole + Post(authoredBy=Person) triangle."

  - id: CQ-019
    file: tests/cq-019.sparql
    kind: select
    expected: "0..n distinct (person, cmc) pairs"
    expected_results_contract: |
      Columns: ?person ?cmc.
      Cardinality: 0..n; DISTINCT applied.
      Integration check: composes the role-bearing pattern (CQ-018) with
      the transitive OEO walk (CQ-015).
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-019.ttl
    entailment_regime: simple
    fixture_rationale: "Composes CQ-018 role-bearer triangle with CQ-015 OEO subtree tag in a single fixture."

  # =====================================================
  # V2 Editorial Extension (CQ-N1..N8, CQ-T1..T3)
  # Added 2026-05-04 by ontology-requirements (V2)
  # =====================================================

  - id: CQ-N1
    file: tests/cq-editorial-N1.sparql
    kind: select
    expected: "0..1 rows; exactly 1 when the story stem corresponds to an imported Narrative"
    expected_results_contract: |
      Columns: ?narrative.
      Cardinality: 0..1.
      Verifies the deterministic IRI rule (Narrative IRI = base + story-stem).
      Test fixture binds story-stem "2026-04-06-tva-nuclear-costs" and asserts
      the corresponding Narrative individual exists.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N1.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect session lands the fixture: declares ei:Narrative class plus an instance with the deterministic IRI."

  - id: CQ-N2
    file: tests/cq-editorial-N2.sparql
    kind: select
    expected: "0..n (post, role) pairs"
    expected_results_contract: |
      Columns: ?post ?role.
      Cardinality: 0..n.
      Forward walk Narrative -> hasNarrativePostMembership -> {memberPost, memberRole}.
      Against a fixture with two memberships (lead + supporting), returns both rows.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N2.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: NarrativePostMembership instances with memberPost + memberRole."

  - id: CQ-N3
    file: tests/cq-editorial-N3.sparql
    kind: select
    expected: "0..n (narrative, role) pairs"
    expected_results_contract: |
      Columns: ?narrative ?role.
      Cardinality: 0..n.
      Reverse walk Post -> Membership -> Narrative; project memberRole.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N3.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: shared between CQ-N2/N3 (forward + reverse over same membership graph)."

  - id: CQ-N4
    file: tests/cq-editorial-N4.sparql
    kind: select
    expected: "0..n Narratives tagged with the bound topic"
    expected_results_contract: |
      Columns: ?narrative.
      Cardinality: 0..n.
      Direct lookup over ei:narrativeAboutTopic against an OEO IRI
      (oeo:OEO_00010439 nuclear power technology).
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N4.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture with at least one Narrative tagged via ei:narrativeAboutTopic to a verified OEO topic IRI."

  - id: CQ-N5
    file: tests/cq-editorial-N5.sparql
    kind: select
    expected: "0..n Narratives applying the bound argument-pattern concept"
    expected_results_contract: |
      Columns: ?narrative.
      Cardinality: 0..n.
      Direct lookup over ei:narrativeAppliesPattern against
      apat:structural-economic-analysis.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N5.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: argument-pattern SKOS scheme + 7 skos:Concept instances + at least one Narrative applying one of them."

  - id: CQ-N6
    file: tests/cq-editorial-N6.sparql
    kind: select
    expected: "0..n distinct experts via UNION over explicit assertion + property-path fallback"
    expected_results_contract: |
      Columns: ?expert.
      Cardinality: 0..n; DISTINCT applied.
      UNION of (a) explicit ei:narrativeMentionsExpert and (b) walk through
      hasNarrativePostMembership/memberPost/authoredBy.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N6.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: optionally asserts ei:narrativeMentionsExpert + always asserts the membership chain; tests both branches of the UNION."

  - id: CQ-N7
    file: tests/cq-editorial-N7.sparql
    kind: select
    expected: "exact_0 rows on a well-formed fixture"
    expected_results_contract: |
      Columns: ?narrative ?post.
      Cardinality: exactly 0.
      Any row is a violation of the SHACL invariant
      sh:qualifiedMaxCount 1 per (Narrative, Post) pair.
      Test runner fails when this query returns > 0 rows.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N7.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: well-formed corpus with at most one membership per (narrative, post) pair (expects 0 violations)."

  - id: CQ-N8
    file: tests/cq-editorial-N8.sparql
    kind: select
    expected: "exact_0 rows on a well-formed fixture; >0 surfaces as SHACL warning"
    expected_results_contract: |
      Columns: ?narrative.
      Cardinality: exactly 0 (well-formed); >0 surfaces as warning.
      Constraint check for at-most-one lead role per Narrative.
      SHACL severity is sh:Warning, not sh:Violation, so test runner
      flags but does not fail when this query returns > 0 rows.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-N8.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: well-formed corpus with single lead membership per Narrative."

  - id: CQ-T1
    file: tests/cq-editorial-T1.sparql
    kind: select
    expected: "1..n rows (at least one prefLabel; OPTIONAL columns may be null)"
    expected_results_contract: |
      Columns: ?topic ?prefLabel ?altLabel ?broader ?narrower.
      Cardinality: 1..n.
      Bound IRI is oeo:OEO_00010427 (solar power technology).
      Cross-product expansion if multiple altLabels or neighbours.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-T1.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: rebuilt OEO topic subset (imports/oeo-topic-subset.ttl) plus supplement scheme(s). Fixture binds against an accepted runtime topic IRI."

  - id: CQ-T2
    file: tests/cq-editorial-T2.sparql
    kind: select
    expected: "1..n IRIs whose skos:notation matches the bound legacy slug"
    expected_results_contract: |
      Columns: ?iri.
      Cardinality: 1..n (1-to-many is allowed for umbrella -> multiple OEO leaves).
      Bound literal is "data-center-demand"; rebinds against any of the
      30 canonical slugs declared in V1's canonicalTopicOrder.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-T2.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: each Skygest supplement IRI carries skos:notation with its legacy topic_slug per scope-v2.md Open Question 4."

  - id: CQ-T3
    file: tests/cq-editorial-granularity.sparql
    kind: select
    expected: "exact_0 rows on a well-formed corpus"
    expected_results_contract: |
      Columns: ?topic.
      Cardinality: exactly 0.
      Any row indicates a granularity-contract violation: a value of
      ei:narrativeAboutTopic that is neither owl:Class nor skos:Concept.
      OWL 2 punning is allowed (an OEO term can be both owl:Class and
      owl:NamedIndividual when used as a value); the FILTER NOT EXISTS
      form handles punning correctly.
    parse_status: ok
    fixture_run_status: pass
    fixture_path: tests/fixtures/cq-editorial-granularity.ttl
    entailment_regime: simple
    fixture_rationale: "V2 architect lands fixture: well-formed corpus where every ei:narrativeAboutTopic value resolves to owl:Class (OEO IRIs) or skos:Concept (supplement IRIs)."

# Loopback triggers recorded for this manifest
#
# None raised. If any parse_status transitions to `fail` in a later session,
# route `sparql_parse` loopback to the sparql-expert skill per
# .claude/skills/_shared/iteration-loopbacks.md § 3.
