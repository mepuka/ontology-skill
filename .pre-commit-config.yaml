repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: check-merge-conflict
      - id: debug-statements

  - repo: local
    hooks:
      - id: validate-turtle
        name: Validate Turtle syntax
        entry: uv run python scripts/validate_turtle.py
        language: system
        files: '\.ttl$'
        types: [file]

      - id: validate-sssom
        name: Validate SSSOM mappings
        entry: uv run python scripts/validate_sssom.py
        language: system
        files: '\.sssom\.tsv$'
        types: [file]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.14.1
    hooks:
      - id: mypy
        additional_dependencies:
          - pandas-stubs>=2.0
          - types-PyYAML>=6.0
          - types-requests>=2.31
        args: [--ignore-missing-imports, src/]
        pass_filenames: false
